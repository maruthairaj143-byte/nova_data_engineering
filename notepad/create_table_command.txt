-- Table: public.raw_client

 DROP TABLE IF EXISTS public.raw_client;

CREATE TABLE IF NOT EXISTS public.raw_client
(
    id bigint,
    first_name text COLLATE pg_catalog."default",
    last_name text COLLATE pg_catalog."default",
    email text COLLATE pg_catalog."default",
    mobile_number bigint,
    gender text COLLATE pg_catalog."default",
    dob text COLLATE pg_catalog."default",
    t_shirt_size text COLLATE pg_catalog."default",
    name_bib text COLLATE pg_catalog."default",
    blood_group text COLLATE pg_catalog."default",
    emergency_contact_name text COLLATE pg_catalog."default",
    emergency_contact_number bigint,
    pincode bigint,
    state text COLLATE pg_catalog."default",
    time_of_registration text COLLATE pg_catalog."default",
    medical_issue text COLLATE pg_catalog."default",
    category text COLLATE pg_catalog."default",
    address text COLLATE pg_catalog."default",
    runner_club text COLLATE pg_catalog."default",
    age bigint,
    distance double precision,
    order_number text COLLATE pg_catalog."default",
    whatsapp_number bigint,
    company text COLLATE pg_catalog."default",
    coupon_code text COLLATE pg_catalog."default",
    payment_id text COLLATE pg_catalog."default",
    order_id text COLLATE pg_catalog."default",
    payment_status text COLLATE pg_catalog."default",
    status text COLLATE pg_catalog."default",
    payment_order_id text COLLATE pg_catalog."default",
    base_fees double precision,
    platform_charges double precision,
    platform_charges_gst double precision,
    registration_gst double precision,
    amount double precision,
    payment_gway_charges double precision,
    payment_gway_charges_gs double precision,
    customer_pays double precision,
    novarace_revenue double precision,
    pg_charges double precision,
    organizer_receives double precision,
	Event_ID text,
    created_by character varying(50) COLLATE pg_catalog."default",
    created_at timestamp without time zone,
    updated_by character varying(50) COLLATE pg_catalog."default",
    updated_at timestamp without time zone,
    batch_id integer
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.raw_client
    OWNER to postgres;
	
	
	
	
	
	CREATE TABLE IF NOT EXISTS raw_result (
    client_id BIGINT,
    mobile_no BIGINT,
    TeamRank BIGINT,
    Team TEXT,
    Bib BIGINT,
    Name TEXT,
    Distance TEXT,
    Gender TEXT,
    ChipTime TEXT,
    StartTime TEXT,
    Split1Distance TEXT,
    Split1 TEXT,
    Split2Distance TEXT,
    Split2 TEXT,
    Split3Distance TEXT,
    Split3 TEXT,
    Split4Distance TEXT,
    Split4 TEXT,

    created_by VARCHAR(50),
    created_at TIMESTAMP,
    updated_by VARCHAR(50),
    updated_at TIMESTAMP,
    Batch_ID INTEGER
);


CREATE TABLE raw_event (
    Event_ID text PRIMARY KEY,
    Event_Name text,
    Event_Date text,
    Marathon_Type text,
    Organizer_Name text,
    Event_Status text,
    Event_Category text,
    Sponsor_Name text,
    Sponsor_Type text,
    Contribution_Amount bigint,
    City text,
    State text,
    Country text,
    Venue_Name text,
    created_by varchar(50),
    created_at timestamp,
    updated_by varchar(50),
    updated_at timestamp,
    Batch_ID integer
);